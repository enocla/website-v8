import {
	AcfBars,
	BedtimeAdjHist,
	BedtimeLongitudinal,
	BedtimeVsAsleep,
	BedtimeVsOverhead,
	BedtimeVsRem,
	BedtimeVsWake,
	SleepDurationHist,
	WeekdayBars,
} from "../../../features/sleep/charts/SleepCharts";
import { sleepStats } from "../../../features/sleep/data/model";

# Data

Tracked with Apple Watch SE2, over ~3 years.

Data exported from [AutoSleep](https://autosleepapp.tantsissa.com/).


## Sleep duration

<SleepDurationHist />

- Mean {sleepStats.asleepMean.toFixed(2)} hours
- Skew {sleepStats.asleepSkew.toFixed(2)}
- Kurtosis {sleepStats.asleepKurt.toFixed(2)}

It's pretty normal. 

The lowest was 18 mins and the highest was 18.5hrs!

## Bedtimes

<BedtimeLongitudinal />

In this one you can see when I was in Hong Kong vs in Canada (not adjusted for timezones).

<BedtimeAdjHist />

- Adjusted std dev {sleepStats.bedAdjStd.toFixed(2)} hours
- Median 12:15am
- IQR 11:13pm – 1:19am

## Bedtime vs sleep duration

<BedtimeVsAsleep />

- r = {sleepStats.regAsleep.r.toFixed(2)}
- Slope {(60 * sleepStats.regAsleep.slopeHrsPerHr).toFixed(1)} min/hour

So sleeping earlier ~ more sleep

## Bedtime vs wake time

<BedtimeVsWake />

- r = {sleepStats.regWake.r.toFixed(2)}
- Slope +{(60 * sleepStats.regWake.slopeHrsPerHr).toFixed(1)} min/hour

You might expect this one to be the same as the previous one, but this also counts time in bed but awake.



## Bedtime vs REM sleep

<BedtimeVsRem />

- r = {sleepStats.regRem.r.toFixed(2)}
- Slope {(60 * sleepStats.regRem.slopeHrsPerHr).toFixed(1)} min/hour
- Early (&lt;11:15pm) {sleepStats.remEarlyMin.toFixed(1)} min vs late (&gt;1:20am) {sleepStats.remLateMin.toFixed(1)} min

You might have heard that [REM sleep is locked to circadian rhythm](https://pubmed.ncbi.nlm.nih.gov/7302463/). Seems to check out, somewhat.

I originally thought it was deep sleep but that has zero correlation, wow!

## Weekly rhythm

<WeekdayBars />

- Friday 7.50 hours, Saturday 7.39 hours
- Mon–Thu 6.81–6.95 hours

So I catch up on sleep on the weekends.

<AcfBars />

- Lag 1: {sleepStats.acfLag1.toFixed(2)}
- Lag 7: {sleepStats.acfLag7.toFixed(2)}
- Lag 14: {sleepStats.acfLag14.toFixed(2)}
- Short-night threshold {sleepStats.tert1.toFixed(2)} hours

I wanted to see if I'd have bursts of little sleep into more sleep to make back the sleep debt, but it doens't seem like it. 


---


Overall nothing surprising. I love running analyses on data, if you have any ideas for what else I could plot here please reach out.


---

This was made with [Tanstack Charts](https://tanstack.com/charts/latest). It's a good library.
